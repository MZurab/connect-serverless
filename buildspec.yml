version: 0.1

phases:
  install:
    commands:
      – npm install -g serverless
  build:
    commands:
      - npm install
  post_build:
    commands:
      – sls deploy

artifacts:
  files:
    – serverless.yml
    – publish.zip
discard-paths: yes